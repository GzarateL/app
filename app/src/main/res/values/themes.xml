<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Tema principal de la aplicación basado en Theme.AppCompat -->
    <style name="Theme.NaveApp" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@android:color/white</item> <!-- Cambiar a blanco -->
        <item name="colorPrimaryVariant">@android:color/white</item>
        <item name="colorOnPrimary">@android:color/black</item>
        <item name="colorSecondary">@color/teal_200</item>
        <item name="android:textColor">@android:color/black</item>
    </style>

    <!-- Tema sin ActionBar para el Splash Screen -->
    <style name="Theme.NaveApp.NoActionBar" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@android:color/white</item> <!-- Cambiar a blanco -->
        <item name="colorPrimaryVariant">@android:color/white</item>
        <item name="colorOnPrimary">@android:color/black</item>
        <item name="colorSecondary">@color/teal_200</item>
    </style>
</resources>
